# Session Starter

You are working in `<absolute repo path>`.

## Goal

`<one concrete objective>`

## First Checks

- Run `git status --short --branch`.
- Fetch or inspect remote state if this task depends on current PR/CI truth.
- Read `AGENTS.md`, `docs/CONTEXT.md`, and the specific docs named below.
- Preserve unrelated local changes.

## Required Reading

- `<path or URL>`
- `<path or URL>`

## Scope

In scope:
- `<item>`

Out of scope:
- `<item>`

## Verification

- `<test/build/check>`
- `<browser/live/CI check if relevant>`
- Secret review if auth, env, deploy, payment, or infra files are touched.

## Completion

Finish with changed files, verification evidence, remaining gates, and any PR or
handoff links. Do not claim readiness beyond the evidence gathered.
